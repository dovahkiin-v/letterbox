"""Letterbox test suite root.

Fixtures, ``conftest.py``, and the ``fake_harness`` test helper land in Phase 1b
(Migration Test Kit per Vision §13.1). Phase 1a leaves this file as a marker so
``pytest`` collects without errors before any tests exist.
"""
